# Dependencies
node_modules/**
node_modules
assets/javascripts/epic_ladder/node_modules/
tmp/

# Build outputs
# Note: Redmine Plugin の配布特性により、以下の運用方針を採用:
#
# [Git管理する (意図的に追跡)]
#   - assets/build/kanban_bundle.*.js        (本番ビルド・ハッシュ付き・圧縮済み 455KB)
#   - assets/build/asset-manifest.json       (マニフェスト)
#   - assets/build/mockServiceWorker.js      (MSW Worker)
#   - assets/build/index.html                (開発用HTML)
#   → プラグインをGitから直接インストールする環境向け (npm非依存)
#   → 本番ビルドを配布することで、配信時のパフォーマンスを最適化
#
# [Git管理しない (このルールで無視)]
#   - assets/build/*.map                     (ソースマップ・デバッグ用)
#   - assets/build/*.LICENSE.txt             (ライセンスファイル)
#   → デバッグ情報は配布に不要
#
# Webpack出力先の無視ルール:
assets/javascripts/epic_ladder/dist/   # 旧出力先 (2025-10-20 削除済み)
assets/build/*.map                   # ソースマップ
assets/build/*.LICENSE.txt           # ライセンスファイル

# Test artifacts (all in Redmine's tmp/)
# Note: tmp/ is already ignored by Redmine core .gitignore
# Including these for clarity:
tmp/test_artifacts/
assets/javascripts/epic_ladder/coverage

# OS files
.DS_Store
!.gitkeep

# Temporary files
temp.html
*.backup
*.bak
*.old

# Vibes scripts
vibes/scripts/.env
vibes/scripts/hooks/.env
vibes/scripts/hooks/lib/discord_users.json
vibes/scripts/hooks/log

# Environment files
.env
.env.local
.env.development
.env.test
.env.production
vibes/scripts/secrets.sample.json5

# License-sensitive external documentation (copied from third-party sites)
vibes/docs/apis/
